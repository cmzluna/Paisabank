{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "strictNullChecks": true // TODO: include this only in test folder
  },
  "baseUrl": ".",
  "paths": {
    "@/*": ["./src/*"]
  }
}
